class PantallaInicio extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Stack(
        fit: StackFit.expand,
        children: [
          // 🌅 Imagen de fondo
          Image.asset(
            'assets/fondo.jpg', // <-- cambia el nombre según tu imagen
            fit: BoxFit.cover,
          ),

          // 🔲 Capa semitransparente para mejorar contraste
          Container(
            color: Colors.black.withOpacity(0.3),
          ),

          // 📝 Contenido centrado
          Center(
            child: Column(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                // 🏷️ Nombre de la aplicación
                Text(
                  'Mi Aplicación Flutter',
                  style: TextStyle(
                    fontSize: 32,
                    fontWeight: FontWeight.bold,
                    color: Colors.white,
                    letterSpacing: 1.5,
                  ),
                ),

                SizedBox(height: 20),

                // 🪧 Mensaje de bienvenida
                Text(
                  '¡Bienvenido!',
                  style: TextStyle(
                    fontSize: 24,
                    color: Colors.white,
                    fontStyle: FontStyle.italic,
                  ),
                ),
              ],
            ),
          ),
        ],
      ),
    );
  }
}
